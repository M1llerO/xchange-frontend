// URL relativo: assume che il backend sia raggiungibile sullo stesso dominio
// del frontend (es. tramite reverse proxy). Se il backend è su un dominio/porta
// diversi, sostituire con l'URL assoluto completo (es. 'https://api.tuodominio.it/api').
export const environment = {
  apiUrl: '/api'
};
